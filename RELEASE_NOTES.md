# PDF 页面提取（Android 版）v1.2.0 发布说明

**发布日期**：2026-09-05

---

## 📦 下载

| 文件 | 说明 | 大小 |
|------|------|------|
| `PdfPageExtractor-v1.2.0.apk` | Android 安装包 | 0.65 MB |

**SHA-256 校验值**：

```
0AE4858AC7BEB4DE65BABBB46C5C22279004165E79914BD2C4F5709485DE95BD
```

> 校验方法（PowerShell）：`Get-FileHash PdfPageExtractor-v1.2.0.apk -Algorithm SHA256`

**系统要求**：Android 5.0（API 21）及以上。首次安装需允许「安装未知来源应用」。

---

## ✨ 本次发布包含

### 核心功能
- **PDF 页面提取**：按页码或页码范围批量提取页面，生成新的 PDF
  - 起始页 + 结束页双输入，结束页留空 = 只提取该一页
  - 支持多组范围同时提取，自动去重
  - 保留原始页面内容与质量，不重新渲染
- **预览选页**（v1.2.0 新增）：网格展示每页缩略图，点按选中/取消，支持全选；确认后自动压缩为连续范围填入输入框
- **Excel 批量粘贴**：在 Excel 中复制两列数据（起始页 / 结束页），在「起始页」输入框中长按粘贴批量填入
- **Excel 模板导入**：内置「下载导入模板」，生成带示例和填写说明的 `.xlsx` 模板，编辑后上传自动读取
- **崩溃日志**：应用崩溃后下次启动弹窗展示日志，便于截图反馈

### 技术特性
- 纯 Kotlin 手写 PDF 解析器：支持经典 xref 表、PDF 1.5 xref 流、对象流（ObjStm）、FlateDecode 压缩流
- **零第三方 PDF 依赖**（移除 PDFBox-Android），应用体积仅约 0.65 MB
- 预览缩略图使用系统 `PdfRenderer`，无新增依赖
- 纯本地处理，不联网、不上传，无需任何网络权限

---

## 🚀 快速上手

1. 选择 PDF 文件（显示总页数）
2. 设置页面范围（预览选页 / 手动输入 / Excel 粘贴 / 模板导入）
3. 点击「提取页面并保存…」

**范围输入示例**（提取第 3 页、第 5 页、第 9~18 页）：

| # | 起始页 | 结束页 |
|---|--------|--------|
| 1 | 3      | （留空）|
| 2 | 5      | （留空）|
| 3 | 9      | 18     |

---

## 🔧 版本历史

- **v1.2.0**：新增预览选页（缩略图网格点选）
- **v1.1.1**：修复启动即闪退（布局控件类型不匹配）
- **v1.1.0**：移除 PDFBox-Android，改用纯 Kotlin 手写解析引擎，解决 Android 14 兼容性问题
- **v1.0.x**：首个版本

## ✅ 已验证场景

- 多范围提取、单页提取、去重合并
- 经典 xref 表 / xref 流 + 对象流（ObjStm）结构 PDF
- 文件头带垃圾字节、嵌套页面树 + 继承属性
- 提取后输出可再次解析且页数正确（单元测试 10 项全部通过）
- xlsx 模板写入 / 读取往返

## ⚠️ 已知限制

- 不支持加密（带密码）的 PDF
- 预览缩略图依赖系统 `PdfRenderer`，个别特殊 PDF 可能无法渲染（不影响手动输入页码提取）

---

## 📄 许可证

本项目：[MIT 许可证](LICENSE)

---

## 🐛 问题反馈

遇到问题请提交 [Issue](../../issues/new/choose)，附上应用版本、手机型号与 Android 版本、复现步骤与错误截图。若应用崩溃，重启后会弹出崩溃日志，请截图一并提供。

**完整变更历史**：见 [CHANGELOG.md](CHANGELOG.md)
